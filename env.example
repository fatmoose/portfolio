# Email Configuration for Contact Form
# Copy this file to .env.local and fill in your values

# Gmail Configuration (recommended)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Alternative: Use environment variables for other email services
# EMAIL_HOST=smtp.yourdomain.com
# EMAIL_PORT=587
# EMAIL_SECURE=false

# Instructions:
# 1. For Gmail: Enable 2-factor authentication and create an app password
# 2. Go to Google Account Settings > Security > 2-Step Verification > App passwords
# 3. Generate a new app password and use it as EMAIL_PASSWORD
# 4. Use your Gmail address as EMAIL_USER 